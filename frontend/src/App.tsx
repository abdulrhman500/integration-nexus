import React, { FC } from 'react';
import { IntegrationForm } from './integration-form';

const App: FC = () => {
  return (
    <div>
      <IntegrationForm />
    </div>
  );
}

export default App;
